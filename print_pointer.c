#include "main.h"

/**
 * print_pointer - a function that prints the address of the input
 * @str: The pointer of the string to be printed
 *
 *Return: Length or error
 */
int print_pointer(unsigned int ptr)
{
	return (print_hexadecimal_small(ptr));
}
